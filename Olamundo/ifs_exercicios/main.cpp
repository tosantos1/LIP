#include <iostream>

using namespace std;

int main()
{
    float a, b, c, delta, x1, x2;

    cout << "Informe os valores de a, b, e c: ";

    cin >> a >> b >> c;

    delta = b*b - 4*a*c;

    if (delta < 0)
    {
        cout << "Equacao nao possui raizes" << end;
    }
    else
    {

    }
    return 0;
}
